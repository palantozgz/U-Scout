# U Scout - Setup Local

## Requisitos
- Node.js 18+

## Pasos
1. `npm install`
2. `npm run dev`
3. Abre http://localhost:3000

## Base de datos
Ya está configurada en Supabase. Las credenciales están en package.json.
Para crear/actualizar tablas: `npm run db:push`
